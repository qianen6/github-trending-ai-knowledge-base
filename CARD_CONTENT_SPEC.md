# 项目卡片中文内容规范

本文定义每个 GitHub Trending 候选仓库的中文项目卡片如何研究、撰写和验收。它约束的是“项目能做什么”，不替代 `SCREENING_RULES.md` 中的代码质量评分。

项目卡片与中文README是两个独立产物：卡片负责简洁概览，README本地化遵循 `README_TRANSLATION_SPEC.md` 并在详情页后半部分展示，禁止用卡片内容冒充README译文。

## 核心原则

1. 每个仓库必须单独阅读和撰写，不允许按类别批量套用功能、优点、使用方式或不足。
2. 项目能力以 README、官方文档和已选取的核心源码/配置为依据；README、依赖、入口、测试、CI、Release 是否存在，只进入质量评分和证据，不充当功能或优点。
3. 解释性文字使用自然中文。项目名以及 GIS、Jupyter、WebAssembly 等必要技术名词可以保留原文，英文说明句必须翻译和解释，不能原样嵌入中文套话。
4. 已收录项目可以复用稳定证据，但必须重新检查卡片内容；旧卡片套话不得跨日复用。
5. 证据不足时只写能够确认的能力，不根据目录名、编程语言、Stars 或类别猜测功能和优点。

## 单仓库生成步骤

### 1. 提取项目事实

从 README 和官方文档中先记录：

- 项目解决的问题；
- 用户输入、核心处理和输出；
- 可直接使用的功能；
- 支持的平台、数据格式、接口和集成；
- README 明确主张的性能、隐私、成本或体验优势；
- 使用条件、资源要求和真实限制。

每条准备展示的功能或优点都要能够回指至少一个 GitHub 证据 URL。测试、CI、依赖和源码入口仍可记录，但放入 `quality.rationale` 和 `evidence_urls`。

### 2. 撰写中文卡片

| 字段 | 必须回答 | 禁止内容 |
|---|---|---|
| `one_line` | 这是什么，解决什么问题 | “某类项目，主要使用某语言实现” |
| `what` | 用户输入、核心工作方式和输出 | “项目围绕英文简介提供实现、文档或工作流” |
| `audience` | 哪类实际用户会用 | 通用的“需要相关方案的开发者” |
| `usage` | 从哪里开始、如何实际使用 | 仅写“按照 README 操作” |
| `features` | 用户能完成哪些任务，至少两条 | README、依赖、源码入口、测试、CI |
| `why` | 独特能力为什么值得关注，并可附趋势事实 | 只复述榜单周期和 Stars |
| `strengths` | 项目自身的优势，至少两条 | Trending、README 清晰度、测试或 CI 证据 |
| `limitations` | 采用时的能力边界、资源要求或适用条件 | “本知识库没有运行项目” |
| `value` | 在目标场景中的实际价值 | P1/P2/P3/P4 等级套话 |

`features` 与 `strengths` 不能是同义改写：前者写能力，后者写这些能力相对传统流程或替代方案带来的好处。

### 3. 单仓库自检

- 删除项目名后，这份卡片是否仍明显只属于该仓库；如果可以无差别贴给同类仓库，必须重写。
- 每条功能是否含用户动作或领域能力，而不是仓库结构。
- 每条优点是否说明跨平台、性能、隐私、易用性、互操作、扩展性或成本收益之一，并有项目证据。
- 是否存在长段未翻译英文、类别套话、评分套话或静态核验边界。

### 4. 批次验收

先把候选批次写入运行目录中的草稿，不直接覆盖正式 `incoming`：

```powershell
python scripts/trending_engine.py validate-cards --root . --input proof/run-YYYY-MM-DD/incoming.candidate.json
```

命令会逐仓库检查中文表达、禁用套话，并拒绝多个仓库复用完全相同的 `features` 或 `strengths`。只有 `CARD VALIDATE PASS` 后，草稿才能原子写入 `incoming/YYYY-MM-DD.json`；`ingest` 会再次执行同一批次校验。

## Codex 可复用提示词

```text
你正在为一个 GitHub 仓库撰写中文项目卡片。只使用输入中提供的仓库元数据、README、官方文档与已选核心文件证据。

先在内部提取：项目解决的问题、用户输入、核心处理、输出、主要能力、平台/接口/集成、明确优势和真实限制。然后只输出 card JSON。

硬约束：
1. 所有解释使用自然中文；必要技术名词可保留原文，不得原样粘贴英文说明句。
2. one_line 直接说明“是什么、能解决什么问题”。
3. what 说明用户输入、核心工作方式和输出。
4. features 写至少两条用户可见能力；不得写 README、依赖、构建清单、源码入口、测试、CI 或 Release。
5. strengths 写至少两条项目自身优势；不得写 Trending、Stars、README 清晰、测试或 CI。
6. limitations 写项目真实边界；不得写“没有安装或运行项目”。
7. why 必须先说明独特能力的价值，趋势事实只能作为补充。
8. 删除项目名后，内容仍须明显针对该项目；不要复用类别模板。
9. 每项陈述必须能由给定证据支持；证据不足时缩小陈述，不猜测。

输出字段必须且只能是：one_line、what、audience、usage、features、why、strengths、limitations、value。
```

## 正反例边界

错误功能：“仓库包含测试或持续集成相关证据。”  
正确功能：“在浏览器内执行地形、水文、激光雷达、遥感和矢量分析。”

错误优点：“当前进入 GitHub Trending 候选池。”  
正确优点：“地理处理完全在本机浏览器内完成，数据无需上传到服务器。”
