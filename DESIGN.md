# GitHub Trending 知识站设计规范 v4

本文件只定义HTML视觉与交互。内容、评分和去重规则不在此重复。

## 方向

- 安静、精确、偏编辑型的开发者知识库。
- 像技术周刊，不像SaaS营销页。
- 避免渐变、玻璃拟态、夸张圆角、装饰性动画和图标堆叠。

## 布局

- 最大内容宽度1120px；桌面页边距32px，移动端16px。
- 首页和日报按日榜、周榜、月榜分区，桌面两列卡片，移动端单列。
- 项目详情单栏约760px；区块间距32–48px，卡片内边距16–20px。
- 前端榜单项目详情在评分与项目链接之后继续展示“中文 README”；非榜单目录项目不显示该章节。保持同一单栏阅读流，代码块允许横向滚动，图片宽度不超过正文容器。

## 字体与颜色

- 正文使用系统无衬线字体；项目名和数字使用系统等宽字体。
- 正文行高1.7，评分数字使用tabular nums。
- 浅色：背景`#F7F7F4`、内容`#FFFFFF`、文字`#1B1D1F`、次文字`#62676D`、边框`#D9DDDF`。
- 强调：日榜`#A54D18`、周榜`#176B5B`、月榜`#3A5A98`。
- 深色跟随 `prefers-color-scheme`，保持WCAG AA对比度。

## 组件

- 顶部导航：首页、最新日报、Markdown Wiki、GitHub数据源。
- 统计条：页面数、去重候选、通过筛选、新增展示。
- 项目卡：项目名、一句话介绍、周期归属、T/Q/V/F和详情链接。
- 项目详情：单栏章节、中文License作用域和GitHub链接。
- 中文 README：在项目详情页内嵌本地化正文，保留标题、列表、表格、代码块、命令与远程图片。
- README渲染支持官方Markdown中的原生HTML、`picture`、远程图片、视频、折叠区块和赞助表格；原生HTML必须经过白名单清洗，禁止脚本、iframe、表单和内联样式。
- 历史日报：按日期倒序列表。

## 交互与QA

- 无前端框架、外部CDN或复杂JavaScript；`file://`可直接浏览。
- 无动画；悬停仅改变边框或背景。
- 375px、768px和1440px宽度无横向溢出。
- 深浅模式清晰，所有相对链接存在，项目卡和详情无空章节。

## v5.1 目录与阅读增强

- 导航新增“全部项目”；静态 HTML 目录包含所有 Catalog entry，并始终保留正常的项目链接。
- 目录提供原生 search input、分类与语言 select、清空筛选按钮和 aria-live 结果数；只加载本地轻量 catalog.js，不请求网络，不依赖框架或 CDN。
- 无 JavaScript 时仍显示全量目录；无结果时说明如何调整筛选，键盘焦点有明确可见轮廓。
- 详情新增可折叠章节目录、最后评估日期和 T/Q/V/F 说明；卡片和 README 的锚点分别命名，避免重复 ID。
- 保留既定配色、深浅主题、阅读宽度与单栏 README；移动端筛选控件自动换行，宽表和代码块仅在区块内部滚动。

