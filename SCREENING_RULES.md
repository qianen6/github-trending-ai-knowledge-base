# Trending 四层筛选规则 v4

本文件是候选、去重、License、评分和面板归属的唯一规则来源。

## 候选与去重

- 候选只来自21个GitHub Trending页面，不做主题预过滤。
- 当天以规范化 `full_name` 为唯一键，合并不同语言榜和不同周期的全部appearances。
- 跨日期以 `catalog.json` 去重；已收录项目只更新信息，不重复新增项目卡或占用新增面板。

## License

读取实际 `LICENSE`、`COPYING`、`NOTICE` 或仓库指定的许可证文件，只记录：

```text
name          许可证名称
scope_zh      中文作用域说明
evidence_urls GitHub证据链接
```

`scope_zh`说明许可证覆盖哪些代码、模型、数据或资产，以及通常要求保留什么声明。License不参与H/Q/V/F和最终状态，不输出双轨、风险等级或合规结论。未声明时只说明没有统一授权作用域。

## H：硬过滤

以下条件必须全部通过：

1. 是规范原仓库，不是Fork、镜像或重复导入。
2. README能说明问题、输入、处理、输出和使用方式。
3. 有与核心主张一致的源码、工具、模型、数据或工作流。
4. README与代码树没有重大矛盾。
5. 有明确安装或运行路径。
6. 不是空仓库、课程作业、SEO仓库或批量模板。
7. 安装脚本没有无关隐藏下载或异常系统修改。
8. 核心依赖仍可获得。

任一失败即 `FAIL`。

## T：趋势分（100）

```text
T = 周榜周期Stars百分位 × 50%
  + 日榜周期Stars百分位 × 20%
  + 月榜周期Stars百分位 × 15%
  + 历史排名变化 × 10%
  + 跨周期出现 × 5%
```

- 百分位基于当天全部去重候选。
- 同周期跨语言榜取最大周期Stars，保留全部来源。
- 无历史排名或页面缺失值时使用中性50，不以0冒充。
- 跨周期分为 `出现周期数 / 3 × 100`。
- 通过线：`T >= 40`。

## Q：静态质量分（100）

| 维度 | 上限 |
|---|---:|
| README与源码一致性 | 20 |
| 核心实现完整度 | 20 |
| 安装与使用说明 | 15 |
| 测试、CI与Release | 20 |
| 文档、示例与错误说明 | 10 |
| 架构与维护性 | 10 |
| 依赖透明度 | 5 |

通过线：`Q >= 60`。本工具只做静态核验，不得声称“已验证好用”。

## V：项目价值分（100）

| 维度 | 上限 |
|---|---:|
| 解决问题的价值 | 20 |
| 相对现有方案的实际提升 | 20 |
| 潜在使用频率 | 15 |
| 工作流完整度 | 15 |
| 互操作性 | 10 |
| API/CLI/插件与扩展性 | 10 |
| 数据、生态或知识复利 | 5 |
| 成本收益比 | 5 |

通过线：`V >= 60`。

层级：`P0`资源或样例、`P1`单点工具、`P2`完整工具或工作流、`P3`生产系统、`P4`基础设施或生态系统。

## 最终状态

```text
F = T × 20% + Q × 45% + V × 35%
```

正式通过必须同时满足 `H=PASS`、`T>=40`、`Q>=60`、`V>=60`、`F>=65`。

- S：`F >= 85`
- A：`75 <= F < 85`
- B：`65 <= F < 75`
- 其他：FAIL

## 面板归属

- 每个尚未收录且正式通过的项目只进入一个面板。
- 归入日、周、月三个周期中百分位最高者；同分按日、周、月顺序。
- 每栏最多5个，按F降序，三个面板全局去重。
- 淘汰项目写入 `rejections/YYYY-MM-DD.json`。

