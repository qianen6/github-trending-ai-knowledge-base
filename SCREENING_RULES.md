# Trending 四层筛选规则 v2

## 第一层：硬过滤 H

结果只有 `PASS` 或 `FAIL`，没有 `CONDITIONAL`。

必须全部通过：

1. 是规范原仓库，不是 Fork、镜像或重复导入。
2. README 清楚说明问题、输入、处理、输出和使用方式。
3. 存在与核心主张一致的源码、Agent Skill、模型或工作流。
4. README 与代码树不存在重大矛盾。
5. 存在明确安装或运行路径。
6. 不是空仓库、课程作业、SEO 仓库或批量模板。
7. 安装脚本不存在与功能无关的隐藏下载或异常系统修改。
8. 核心依赖仍可获得。

任一失败即 `FAIL`，停止后续评分。

### License 双轨

必须读取仓库中的实际 `LICENSE`、`COPYING`、NOTICE、模型/数据卡和资产归属文件；GitHub API 的 `NOASSERTION` 只表示平台未自动识别，不能直接当成“没有许可证”。

| 情况 | 学习科研 | 工程落地 |
|---|---|---|
| 明确兼容的宽松许可证 | PASS | PASS |
| GPL/AGPL 等 copyleft | PASS＋风险标签 | 按集成和分发方式 PASS/FAIL |
| 非商业许可证 | PASS＋`NONCOMMERCIAL` | FAIL |
| 自定义或无法识别 | PASS＋`LICENSE_UNRECOGNIZED` | FAIL |
| 没有 License | PASS＋`LICENSE_MISSING` | FAIL |
| 代码/模型/数据许可证冲突 | PASS，可研究 | FAIL |
| 输出权利不明确 | PASS＋`OUTPUT_RIGHTS_UNCLEAR` | FAIL |
| 源码许可证明确，但附带资产要求署名 | PASS＋`ASSET_ATTRIBUTION_REQUIRED` | 满足署名与资产条款时PASS |

## 第二层：Trending 趋势分 T（100）

```text
T = 周榜周期Stars百分位 × 50%
  + 日榜周期Stars百分位 × 20%
  + 月榜周期Stars百分位 × 15%
  + 排名变化与连续上榜 × 10%
  + 跨周期出现 × 5%
```

### 计算规则

- 周、日、月周期 Stars 分别在当日去重候选中计算百分位。
- 同一仓库出现在多个语言榜时，同周期取页面展示值的最大值；保留全部来源页面。
- 首次采集没有历史排名时，排名变化项使用中性 50 分。
- 后续采集根据同周期、同语言页面的平均排名改善与连续出现计算排名项。
- 跨周期项按 Today/Week/Month 出现数量计算：`出现周期数 / 3 × 100`。
- 页面缺失值记 `null`，对应百分位采用中性 50，不以 0 冒充事实。
- 通过线：`T >= 40`；否则 `FAIL`。

## 第三层：静态质量分 Q（100）

本工具不安装、不执行候选仓库。

| 维度 | 上限 |
|---|---:|
| README 与源码一致性 | 20 |
| 核心实现完整度 | 20 |
| 安装与使用说明 | 15 |
| 测试、CI 与 Release | 20 |
| 文档、示例与错误说明 | 10 |
| 架构与维护性 | 10 |
| 依赖透明度 | 5 |

通过线：`Q >= 60`；否则 `FAIL`。

静态核验不能证明真实好用，输出不得使用“已验证好用”。

## 第四层：AI价值分 V（100）

| 维度 | 上限 |
|---|---:|
| 问题价值与使用频率 | 20 |
| 相比传统方案的提升 | 20 |
| AI 必要性 | 15 |
| 工作流完整度 | 15 |
| 模型与供应商可替换性 | 10 |
| API/CLI/MCP 与扩展性 | 10 |
| 数据和知识复利 | 5 |
| 成本收益比 | 5 |

通过线：`V >= 60`；否则 `FAIL`。

AI层级：

- `L0`：AI贴牌。
- `L1`：单一AI功能点。
- `L2`：完整AI工作流。
- `L3`：AI生产系统。
- `L4`：AI基础设施或复利系统。

## 最终分

```text
F = T × 20% + Q × 45% + V × 35%
```

正式收录必须同时满足：

```text
H(学习科研) = PASS
T >= 40
Q >= 60
V >= 60
F >= 65
```

- `S`：F >= 85
- `A`：75 <= F < 85
- `B`：65 <= F < 75
- 其他：FAIL

每天最多突出展示 5 个正式通过项目；NEW_HOT 与 REVIVED_HOT 分栏。任一层失败写入 rejections。
