# Trending 四层筛选规则 v3

## 候选范围与去重

- 自动候选只来自 GitHub Trending，不建立全站仓库池。
- 不进行 AI、语言用途或行业主题预过滤；21个页面去重后的每个仓库都进入评估。
- 当天以规范化 `owner/repo`（`full_name`）为唯一键，日榜、周榜、月榜和不同语言榜之间只保留一份仓库记录，同时保留全部页面出现证据。
- 跨日期以同一个 `full_name` 作为长期唯一键；已经正式收录的仓库只更新趋势与最后评估时间，不重复新增项目卡，也不再次占用日报新增展示名额。
- License只保留名称、GitHub证据链接和中文作用域说明，不参与筛选、评分或工程/科研双轨判断。

## License作用域说明

- 读取实际 `LICENSE`、`COPYING`、`NOTICE` 或仓库明确指定的许可证文件。
- 只输出三个字段：许可证名称、中文作用域说明、证据链接。
- 中文说明回答“许可证覆盖哪些代码、模型、数据或资产，以及通常要求保留什么声明”。
- 不输出科研PASS、工程PASS、风险等级或合规结论。
- 未声明许可证时，写明“仓库未声明统一许可证，本字段只记录没有统一授权范围”，但不因此改变H/Q/V/F或最终状态。

## 第一层：硬过滤 H

结果只有 `PASS` 或 `FAIL`。

必须全部通过：

1. 是规范原仓库，不是 Fork、镜像或重复导入。
2. README 清楚说明问题、输入、处理、输出和使用方式。
3. 存在与核心主张一致的源码、工具、模型、数据或工作流。
4. README 与代码树不存在重大矛盾。
5. 存在明确安装或运行路径。
6. 不是空仓库、课程作业、SEO 仓库或批量模板。
7. 安装脚本不存在与功能无关的隐藏下载或异常系统修改。
8. 核心依赖仍可获得。

任一失败即 `FAIL`。

## 第二层：Trending 趋势分 T（100）

```text
T = 周榜周期Stars百分位 × 50%
  + 日榜周期Stars百分位 × 20%
  + 月榜周期Stars百分位 × 15%
  + 历史排名变化 × 10%
  + 跨周期出现 × 5%
```

- 日、周、月周期 Stars 分别在当日全部去重候选中计算百分位。
- 同一仓库出现在多个语言榜时，同周期取页面展示值的最大值，并保留全部来源页面。
- 首次采集没有历史排名时，排名变化项使用中性 50 分。
- 后续采集对照上一次快照的平均排名。
- 跨周期项为 `出现周期数 / 3 × 100`。
- 页面缺失值记 `null`，对应百分位使用中性 50，不以 0 冒充事实。
- 通过线：`T >= 40`。

## 第三层：静态质量分 Q（100）

本工具不安装、不执行候选仓库。

| 维度 | 上限 |
|---|---:|
| README 与源码一致性 | 20 |
| 核心实现完整度 | 20 |
| 安装与使用说明 | 15 |
| 测试、CI 与 Release | 20 |
| 文档、示例与错误说明 | 10 |
| 架构与维护性 | 10 |
| 依赖透明度 | 5 |

通过线：`Q >= 60`。

静态核验不能证明真实好用，输出不得使用“已验证好用”。

## 第四层：项目价值分 V（100）

| 维度 | 上限 |
|---|---:|
| 解决问题的价值 | 20 |
| 相比现有方案的实际提升 | 20 |
| 潜在使用频率 | 15 |
| 工作流完整度 | 15 |
| 互操作性 | 10 |
| API/CLI/插件与扩展性 | 10 |
| 数据、生态或知识复利 | 5 |
| 成本收益比 | 5 |

通过线：`V >= 60`。

项目层级：

- `P0`：资源展示、导航或概念样例。
- `P1`：解决一个明确问题的单点工具。
- `P2`：完整工具或端到端工作流。
- `P3`：可进入持续使用的生产系统。
- `P4`：基础设施、平台或具有生态复利的系统。

## 最终分

```text
F = T × 20% + Q × 45% + V × 35%
```

正式通过必须同时满足：

```text
H = PASS
T >= 40
Q >= 60
V >= 60
F >= 65
```

- `S`：F >= 85
- `A`：75 <= F < 85
- `B`：65 <= F < 75
- 其他：FAIL

## 日榜、周榜、月榜面板

- 每个正式通过且尚未收录的仓库只进入一个面板。
- 根据日、周、月三个周期的百分位，把仓库归入相对表现最强的周期；同分时按日榜、周榜、月榜顺序归属。
- 每个面板最多展示 5 个项目，并按 F 从高到低排序。
- 一个仓库即使同时进入多个周期或语言榜，也不会跨面板重复展示。
- 已经存在于长期目录的仓库继续更新趋势，但不会再次作为“新增项目”展示。
- 任一层失败写入 `rejections/YYYY-MM-DD.json`，不在读者日报展示淘汰原因。
